{
    "builds": [
        {
            "src": "backend/app.py",
            "use": "@vercel/python"
        }
    ],
    "rewrites": [
            {
            "src": "/(.*)",
            "dest": "backend/app.py"
        }
    ],
    "env": {
        "PYTHONPATH" : "backend"
    }
}